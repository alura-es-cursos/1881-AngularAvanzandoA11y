import { FocusBackDirective } from './focus-back.directive';

describe('FocusBackDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusBackDirective();
    expect(directive).toBeTruthy();
  });
});
